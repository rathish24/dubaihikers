# Firebase

This directory contains the planned Firebase project configuration.

Before enabling Firebase:

1. Create separate development and production Firebase projects.
2. Install and authenticate the Firebase CLI.
3. Add the approved project aliases to `.firebaserc`.
4. Review Firestore rules against the final authentication and booking design.
5. Never commit service-account keys or production secrets.

The web prototype does not currently connect to Firebase.
